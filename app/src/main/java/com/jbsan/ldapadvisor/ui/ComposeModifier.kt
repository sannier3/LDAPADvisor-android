package com.jbsan.ldapadvisor.ui

import androidx.compose.ui.Modifier

/**
 * Alias so call sites can write `ComposeModifier.fillMaxSize()` without fighting the
 * `androidx.compose.ui.modifier` package vs `androidx.compose.ui.Modifier` class clash.
 */
typealias ComposeModifier = Modifier
